[www]
user = www-data
group = www-data

; Listen on TCP so nginx can reach it at 127.0.0.1:9000
listen = 9000

pm = dynamic
pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

; Pass real IP from nginx fastcgi headers
clear_env = no
